datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model ScheduledEvent {
  id        String   @id @default(cuid())
  name      String
  startTime String
  duration  String?  // Optional field mapping
  createdAt DateTime @default(now())
}